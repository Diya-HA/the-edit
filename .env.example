# Copy to .env for local development. .env is gitignored.
#
# Matches the credentials in docker-compose.yml. In production the platform
# provides DATABASE_URL itself — never set it in the repo, and never commit
# a real one here.
DATABASE_URL="postgresql://theedit:theedit@localhost:5432/theedit?schema=public"
